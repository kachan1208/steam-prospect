/**
 * Radar — the opportunity feed that becomes the index route (mockup 3a).
 * Placeholder: the shell, route and nav entry land first so page work can proceed in
 * parallel without every agent editing App.tsx.
 */
export default function Radar() {
  return (
    <div className="mx-auto max-w-[1180px]">
      <h1 className="text-[32px]">Radar</h1>
      <p className="mt-2 text-sm text-ink-secondary">Opportunity feed — implementation in progress.</p>
    </div>
  );
}
