/**
 * Watchlist — the tracked niches and games with alert rules (mockup 4f).
 * Placeholder: the shell, route and nav entry land first so page work can proceed in
 * parallel without every agent editing App.tsx.
 */
export default function Watchlist() {
  return (
    <div className="mx-auto max-w-[1180px]">
      <h1 className="text-[32px]">Watchlist</h1>
      <p className="mt-2 text-sm text-ink-secondary">Watchlist — implementation in progress.</p>
    </div>
  );
}
