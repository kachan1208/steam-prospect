duckdb>=1.5
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
pydantic>=2.7
pydantic-settings>=2.3
python-multipart>=0.0.9
anthropic>=0.40

# Hosted MCP: serve mcp/prospect_mcp.py at /mcp over Streamable HTTP (PROSPECT_ENABLE_MCP).
mcp>=1.2

# O3 observability: /metrics (VictoriaMetrics/Prometheus scrape) + env-gated Sentry.
prometheus-fastapi-instrumentator>=7.0
sentry-sdk>=2.0

# Test-only (api/tests/) — see Track O2 in the production/marketing plan.
pytest>=8.0
httpx>=0.27
